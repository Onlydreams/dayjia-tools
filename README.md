# dayjia-tools

Static tools hub for `tool.dayjia.com`.

## Structure

- `index.html`: tools directory landing page.
- Each tool should live in its own subdirectory, for example `down-jacket-helper/`.
